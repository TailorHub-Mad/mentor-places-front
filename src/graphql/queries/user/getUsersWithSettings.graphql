query GetUsersWithSettings($filter: users_filter) {
  users(filter: $filter) {
    id
    first_name
    family_name
    family_name_2
    middle_name
    birth_date
    postal_code
    city
    address
    email
    role
  }
}
