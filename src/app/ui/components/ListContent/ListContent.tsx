import type { FC } from 'react'

interface IListContentProps {
  list: { title: string; description: string }[]
}

const ListContent: FC<IListContentProps> = () => {
  return <div></div>
}

export default ListContent
