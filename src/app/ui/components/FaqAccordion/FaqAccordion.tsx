import type { FC } from 'react'

interface IFaqAccordionProps {
  className?: string
}

const FaqAccordion: FC<IFaqAccordionProps> = ({}) => {
  return <div class="faq-accordion">FaqAccordion</div>
}

export default FaqAccordion
